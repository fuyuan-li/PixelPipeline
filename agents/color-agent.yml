# DEPRECATED — Color-only review has been removed from this pipeline.
#
# The design philosophy of this project is Design System Migration, not colour
# linting. Semantic component inference and replacement is handled entirely by
# the Component Classifier in flows/figma-review.yml.
#
# This file is kept for reference only. It is NOT used in any active flow.
name: "Figma Color Review Agent (deprecated)"
description: >
  DEPRECATED. Previously reviewed detached colours against design system tokens.
  Colour review is no longer part of the DS Migration pipeline — semantic
  component classification and replacement covers the same goal more completely.
public: false
system_prompt: |
  This agent is deprecated. See flows/figma-review.yml for the active pipeline.
tools: []
